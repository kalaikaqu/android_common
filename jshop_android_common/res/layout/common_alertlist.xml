<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="640dp"
    android:layout_height="650dp"
    android:background="@drawable/alertbgwithconer"
    android:padding="32dp">
    
    <ImageView 
        android:id="@+id/list_logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/common_logo_inwhitebg"/>
    <TextView
        android:id="@+id/list_count"
        android:text="共计:129元"
        android:textColor="#ffc71111"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="22dp"
        android:layout_alignParentBottom="true">
        
    </TextView>
    <ListView 
        android:id="@+id/common_listview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/list_logo"
        android:layout_above="@id/list_count"
        android:layout_marginTop="32dp">
        
    </ListView>
	

        
</RelativeLayout>